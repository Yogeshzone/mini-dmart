package com.dmart.mini_dmart.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class JpaConfig {

    // JPA configuration is handled automatically by Spring Boot.
    // Entity-specific configuration is defined using JPA annotations
    // in the entity classes.
}